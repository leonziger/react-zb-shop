import React from 'react';
import { Layout} from 'antd';
import '../../App.css';

export const Cart = () => {
  return(
    <Layout className="mainContent">
      Тут будет корзина
    </Layout>
  );
};